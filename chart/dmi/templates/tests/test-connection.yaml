#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ include "dmi.fullname" . }}-test-connection"
#  labels:
#    {{- include "dmi.labels" . | nindent 4 }}
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['{{ include "dmi.fullname" . }}:{{ .Values.service.port }}']
#  restartPolicy: Never
